# Skills

Published skills live here, one directory per skill. Each directory must contain a valid `SKILL.md` whose frontmatter `name` matches the directory name.

This directory is intentionally empty while the initial collection is being reviewed. Do not add private or account-specific skills without a separate public-safety and licensing review.
