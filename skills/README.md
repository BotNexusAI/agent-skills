# Skills

Published skills live here, one directory per skill. Each directory must contain a valid `SKILL.md` whose frontmatter `name` matches the directory name.

Published skills currently include:

- `linear-ops` — portable Linear issue and project workflow guidance with an
  optional read-only helper.

Do not add private or account-specific skills without a separate public-safety
and licensing review.
